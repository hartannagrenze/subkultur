// BubbleChart.js
import React from 'react';

const BubbleChart = ({ data }) => {
  // Hier kommt die Logik für die Darstellung der Bubbles
  return (
    <div>
      {/* Hier renderst du die Bubbles basierend auf den Daten */}
    </div>
  );
};

export default BubbleChart;
